import { WeeklyNetSalaryPipe } from './weekly-net-salary.pipe';

describe('WeeklyNetSalaryPipe', () => {
  it('create an instance', () => {
    const pipe = new WeeklyNetSalaryPipe();
    expect(pipe).toBeTruthy();
  });
});
